# ── Deep learning (segmentation model) ───────────────────────────────────────
torch
pytorch-lightning
torchmetrics
segmentation-models-pytorch
monai
albumentations

# ── Numerics & image I/O ─────────────────────────────────────────────────────
numpy
scipy
scikit-image
tifffile          # TIFF backend used by skimage.io for the slice dataset
Pillow

# ── Post-analysis (clustering / machine learning) ────────────────────────────
scikit-learn      # t-SNE embedding
hdbscan           # density-based chamber clustering
pandas

# ── Morphometric quantification ──────────────────────────────────────────────
porespy           # local thickness, pore-network metrics
openpyxl          # .xlsx export from the quantification notebooks

# ── Annotation tool (annotation/) ────────────────────────────────────────────
# The annotator itself needs no torch: only these two on top of numpy, scipy,
# scikit-image and Pillow above.
nicegui           # web UI framework serving the annotator
opencv-python     # brush rasterisation onto the mask

# ── Visualisation & logging ──────────────────────────────────────────────────
matplotlib
plotly
seaborn
tensorboard
click
tqdm

# ── Optional: notebooks ──────────────────────────────────────────────────────
scikit-posthocs   # post-hoc statistical tests in the quantification notebooks
