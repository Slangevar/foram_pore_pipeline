# Validation Checks (Experimental)

This folder contains ad-hoc validation slice prediction scripts used for manual checks.

- `predict_val_slice.py`
- `predict_val_slice_single.py`

These are not part of the default SLURM production pipeline.
