# Dependencies for the annotation tool only.
# The tool is standalone: it has no torch/model dependencies, since the
# training and inference code was removed (see README.md in this folder).
#
#   pip install -r annotation/requirements.txt

nicegui           # web UI framework serving the annotator
opencv-python     # brush rasterisation onto the mask
numpy
scipy             # slice interpolation (map_coordinates)
scikit-image      # TIFF read/write
Pillow            # canvas image handling
